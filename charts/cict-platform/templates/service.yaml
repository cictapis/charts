apiVersion: v1
kind: Service
metadata:
  name: {{ include "cict-platform.fullname" . }}
  labels:
    {{- include "cict-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cict-platform.selectorLabels" . | nindent 4 }}
