apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cict-datacore.fullname" . }}-test-connection"
  labels:
    {{- include "cict-datacore.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cict-datacore.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
