apiVersion: v1
kind: Service
metadata:
  name: {{ include "cict-datacore.fullname" . }}
  labels:
    {{- include "cict-datacore.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cict-datacore.selectorLabels" . | nindent 4 }}
